CREATE TABLE IF NOT EXISTS stats (uuid VARCHAR(36), coins INTEGER(11) DEFAULT 0, score INTEGER(11) DEFAULT 0, kills INTEGER(11) DEFAULT 0, deaths INTEGER(11) DEFAULT 0, kits TEXT DEFAULT 'Standard;', PRIMARY KEY(uuid));
CREATE TABLE IF NOT EXISTS users (uuid VARCHAR(36), username VARCHAR(16), PRIMARY KEY (uuid));